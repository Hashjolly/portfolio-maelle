---
import Layout from '@/layouts/Layout.astro';
import Container from '@/components/ui/Container.astro';
import Section from '@/components/ui/Section.astro';
import Button from '@/components/ui/Button.astro';
import { siteData } from '@/data/site';
import { getFeaturedProjects } from '@/data/projects';
import { Mail, MapPin, Calendar, MessageCircle } from 'lucide-astro';

const featuredProjects = getFeaturedProjects();
---

<Layout>
  <style>
    .bubble {
      position: absolute;
      border-radius: 9999px;
      opacity: 0.7;
      animation: bubbleFloat 6s ease-in-out infinite alternate;
      will-change: transform, opacity;
    }
    @keyframes bubbleFloat {
      0% {
        transform: translateY(0) scale(1);
        opacity: 0.7;
      }
      50% {
        transform: translateY(-30px) scale(1.1);
        opacity: 1;
      }
      100% {
        transform: translateY(-60px) scale(0.95);
        opacity: 0.6;
      }
    }
  </style>
  <div class="min-h-screen animate-fade-in bg-gradient-to-br from-cream via-sand-50 to-sand-100">
    <!-- Navigation -->
    <nav class="fixed top-0 z-50 w-full border-b border-sand-200 bg-cream/90 backdrop-blur-sm">
      <Container class="py-4">
        <div class="flex items-center justify-between">
          <a
            href="/"
            class="font-display text-xl font-bold text-primary transition-colors hover:text-sand-600"
          >
            {siteData.name}
          </a>
          <div class="hidden items-center space-x-8 md:flex">
            <a href="/parcours" class="text-dark transition-colors hover:text-primary">Parcours</a>
            <a href="/projets" class="text-dark transition-colors hover:text-primary">Projets</a>
          </div>
        </div>
      </Container>
    </nav>

    <!-- Hero Section -->
    <Section class="animate-slide-up pb-20 pt-32">
      <Container
        class="relative z-10 flex animate-fade-in flex-col items-center gap-12 md:flex-row"
      >
        {/* Bulles flottantes décoratives */}
        <div class="pointer-events-none absolute inset-0 z-0">
          <div
            class="bubble bg-primary/30"
            style="left:-5%;top:20%;width:40px;height:40px;animation-delay:0s"
          >
          </div>
          <div
            class="bubble bg-sand-200/100"
            style="left:105%;top:65%;width:65px;height:65px;animation-delay:0.5s"
          >
          </div>
          <div
            class="bubble bg-atlantic/30"
            style="left:60%;top:90%;width:110px;height:110px;animation-delay:1s"
          >
          </div>
          <div
            class="bubble bg-salicornes/30"
            style="left:-10%;top:60%;width:85px;height:85px;animation-delay:1.5s"
          >
          </div>
          <div
            class="bubble bg-purple/30"
            style="left:55%;top:15%;width:55px;height:55px;animation-delay:2s"
          >
          </div>
        </div>
        <div class="mx-auto max-w-4xl text-left">
          <h1 class="mb-8 font-display text-5xl font-bold text-dark md:text-6xl">
            Petite
            <span class="block text-primary">introduction</span>
          </h1>
          <p class="mx-auto mb-4 max-w-2xl text-justify text-xl leading-relaxed text-dark/80">
            Native de Charente-Maritime, j'ai posé mes valises d'abord à Angoulême puis à Bordeaux
            pour mes études. Fraîchement diplômée, je suis de retour sur la côte charentaise. Je
            suis chargée de projet en médiation territoriale et géographe de formation. Passionnée
            par le monde qui m'entoure, j'aime comprendre le fonctionnement des territoires et
            oeuvrer à leur développement.
            <p class="mx-auto mb-8 max-w-2xl text-justify text-xl leading-relaxed text-dark/80">
              De nature joviale, j’aime rire, me divertir et j'ai soif d'apprendre. Je suis une
              personne entière qui apprécie que les choses soient dites sans ambigüité. En quelques
              mots, je suis quelqu'un de responsable, méthodique et consciencieuse.
            </p>
            <p class="mx-auto mb-8 max-w-2xl text-justify text-xl leading-relaxed text-dark/80">
              Pendant mon temps libre, je suis une artiste dans l'âme. J'aime m'exprimer à travers
              la peinture, le dessin et la création de bougies. Ces passions me permettent de
              cultiver ma créativité et d'apporter une touche artistique à mes projets
              professionnels.
            </p>
          </p>
          <div class="mt-4 flex flex-col justify-center gap-4 sm:flex-row">
            <Button href="/projets" variant="primary" size="lg"> Découvrir mes projets </Button>
            <Button href="mailto:maellelanglais10@hotmail.fr" variant="outline" size="lg">
              <MessageCircle class="h-5 w-5" />
              Contactez moi
            </Button>
          </div>
        </div>
        <img
          src="/images/photo_de_profil.png"
          alt="Maëlle Langlais"
          class="h-auto w-96 rounded-full border-4 border-white shadow-md"
        />
      </Container>
    </Section>

    <!-- Featured Projects Preview -->
    <Section class="bg-white py-20">
      <Container>
        <div class="mb-16 text-center">
          <h2 class="mb-4 font-display text-3xl font-bold text-dark md:text-4xl">Projets phares</h2>
        </div>

        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {
            featuredProjects.map((project) => (
              <div class="overflow-hidden rounded-2xl bg-cream shadow-sm transition-shadow hover:shadow-lg">
                <div class="aspect-video overflow-hidden bg-sand-200">
                  <img src={project.cover} alt={project.title} class="h-full w-full object-cover" />
                </div>
                <div class="p-6">
                  <div class="mb-3 flex flex-wrap gap-2">
                    {project.tags.slice(0, 2).map((tag) => (
                      <span class="rounded-full bg-primary/10 px-3 py-1 text-sm text-primary">
                        {tag}
                      </span>
                    ))}
                  </div>
                  <h3 class="mb-2 font-display text-xl font-semibold text-dark">{project.title}</h3>
                  <p class="mb-4 text-sm text-dark/70">{project.summary}</p>
                  <Button href={`/projets/${project.slug}`} variant="outline" size="sm">
                    Voir le projet
                  </Button>
                </div>
              </div>
            ))
          }
        </div>

        <div class="mt-12 text-center">
          <Button href="/projets" variant="primary"> Voir tous les projets </Button>
        </div>
      </Container>
    </Section>

    <!-- Footer -->
    <footer class="bg-dark py-12 text-white">
      <Container>
        <div class="mb-8 flex justify-between">
          <div>
            <h4 class="mb-4 font-semibold">Navigation</h4>
            <ul class="space-y-2 text-white/70">
              <li><a href="/parcours" class="transition-colors hover:text-white">Parcours</a></li>
              <li><a href="/projets" class="transition-colors hover:text-white">Projets</a></li>
              <li>
                <a
                  href="mailto:maellelanglais10@hotmail.fr"
                  class="transition-colors hover:text-white">Contact</a
                >
              </li>
            </ul>
          </div>

          <div
            class="flex flex-col items-center justify-between border-t border-white/10 pt-8 text-sm text-white/60 md:flex-row"
          >
            <p>&copy; 2024 {siteData.name}. Tous droits réservés.</p>
            <div class="mt-4 flex space-x-6 md:mt-0">
              <a href="/mentions-legales" class="transition-colors hover:text-white"
                >Mentions légales</a
              >
            </div>
          </div>
        </div>
      </Container>
    </footer>
  </div>
</Layout>
