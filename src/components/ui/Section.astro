---
export interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<section class={`animate-fade-in ${className}`} id={id}>
  <slot />
</section>
